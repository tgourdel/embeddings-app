bundle:
  name: embeddings-app

variables:
  serving_endpoint_name:
    description: Serving endpoint name

resources:
  apps:
    app:
      name: 'embeddings-app'
      description: 'Generate text embeddings with Databricks Foundation Models'
      source_code_path: ./

      # Start the app automatically on deploy.
      lifecycle:
        started: true
      user_api_scopes:
        - serving.serving-endpoints

      # The resources which this app has access to.
      resources:
        - name: serving-endpoint
          serving_endpoint:
            name: ${var.serving_endpoint_name}
            permission: CAN_QUERY

targets:
  default:
    default: true
    workspace:
      host: https://data-ai-lakehouse.cloud.databricks.com

    variables:
      serving_endpoint_name: databricks-gte-large-en
